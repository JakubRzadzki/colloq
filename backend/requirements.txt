# 0.137+ wraps included routers in _IncludedRouter, which slowapi's middleware
# (0.1.9/0.1.10) cannot look into, so default rate limits would silently stop
# applying to router endpoints. tests/test_rate_limits.py guards this.
fastapi==0.136.3
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23
pydantic[email]==2.13.5
pydantic-settings==2.15.0
# PyJWT — without cryptography/Rust (python-jose required Rust on Python 3.14)
PyJWT==2.8.0
bcrypt==4.3.0
python-multipart==0.0.32
psycopg2-binary>=2.9.9
alembic>=1.13.0
slowapi==0.1.9
